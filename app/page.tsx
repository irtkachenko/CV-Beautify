export default function HomePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1 style={{ fontSize: 24, fontWeight: 600 }}>Next.js is connected</h1>
      <p style={{ marginTop: 12 }}>
        Next API routes will replace the Express server. UI migration from Vite will follow.
      </p>
    </main>
  );
}
