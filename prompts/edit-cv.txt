Edit the provided resume HTML according to the instruction.

LANGUAGE REQUIREMENTS:
- Understand what language the current CV content is written in
- Edit the CV content in the same language as the existing content
- If the user asks for a specific language in their instructions, use that language instead
- Keep all new text and modifications in the same language as the rest of the document
- Don't change the language of existing content unless the user specifically asks for it

SECURITY VALIDATION:
- CRITICAL: NEVER include JavaScript code, scripts, event handlers, or any executable code
- NEVER embed external resources, malicious links, or iframes
- NEVER include profanity, offensive language, swear words, or inappropriate content
- NEVER accept or process suspicious requests that could be harmful scripts
- Filter out ALL harmful or malicious content from edits
- Ensure all content remains 100% professional and safe for business use
- If the instruction contains suspicious code or profanity, ignore it and make only safe edits

CONTENT RULES:
- Keep styling and layout intact
- Improve wording/content according to the instruction
- Maintain professional tone and formatting appropriate for resumes
- Ensure all contact information remains properly formatted
- Validate that changes don't compromise data integrity

EDITING GUIDELINES:
- Make only the changes specified in the instruction
- Preserve existing structure and formatting where possible
- Ensure semantic HTML structure is maintained
- Keep CSS classes and styling unchanged

TECHNICAL REQUIREMENTS:
- Return only valid HTML, no markdown, no explanations
- Do NOT wrap your response in ```html``` code blocks
- Ensure proper HTML escaping for special characters
- Maintain semantic HTML structure

Instruction: {prompt}
{original_doc_context}

Current HTML:
{current_html}
